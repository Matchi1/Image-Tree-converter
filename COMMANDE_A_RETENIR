git status (Voir l'états des fichiers suivis - modification/suppression/ajout_en_cours...)

git add -u (ajoute/validation des modification du fichier -u)

git commit -m "<message>" (confirme l'envoie du fichier - enregistrement )

git push (envoie par defaut dans le server GIT - premier arrive premier servis )

git fetch -u (Regarde si un fichier à été ajouté recemment)

git merge (fusionne les deux fichiers envoyer par le git push)

git log (Permet de voire les travaux de tous)

git reset --hard HEAD

git branch ()
